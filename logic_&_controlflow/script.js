// Tenery operator is like a twin brother of if statement
//  For example

// if (condition) {
//     //execute this block if the condition is true
// } else {
//     //Execute this block if not true
// }

// //Now to present same thing using a tenary operatore '?'

// condition ?  //execute this block if the condition is true : //Execute this block if not true;

// //Use case

/* const age = 20;

if (age >= 20) {
    console.log('You can join the club');
} else {
    console.log('You are underage, Grow up')
}

age < 20 ? console.log('You can join the club') : console.log('You are underage, Grow up') */

/* For loop
for loop has a structure which it is written

for(initialisation; condition; finalexpression) {
    block of code that can be console.log statement
}  

The use case is written below*/

for(let i = 0; i < 10; i++ ) {
    console.log(i);
}  